<h2><%= @university.name_original %></h2>
<h4>Name (English): <%= @university.name_english %></h4>
<h4>Website: <%= @university.website %></h4>
<h4>Contact: <%= @university.contact %></h4>
<h4>City: <%= link_to @university.city.name, @university.city %> (<%= link_to @university.city.country.name, @university.city.country %>)</h4>
<br>
<h4>List of Pages</h4>
<% if @university.pages.empty? %>
<em> No Pages</em>
<br>
<% else %> 
<% @university.page.each do |page| %>
	<%= link_to university.pages.title, university.pages %>
<% end %>	
<br>
<% end %>
<%= link_to "New Page", {new_page_path, :university_id => @university.id} %><br>
<br>
<%= link_to 'Edit profile', edit_university_path(@university) %><br>
<%= link_to "Delete", 
	@university, 
	:method => :delete,
	:confirm => "Are you sure you want to delete this university?" %>
<br>
<%= link_to 'Back', {:action => 'index'} %>