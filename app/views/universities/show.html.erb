<h2><%= @university.name_original %></h2>
<h4>Name (English): <%= @university.name_english %></h4>
<h4>Website: <%= @university.website %></h4>
<h4>Contact: <%= @university.contact %></h4>
<h4>City (Country): <%= link_to @university.city.name, @university.city %> (<%= link_to @university.city.country.name, @university.city.country %>)</h4>
<%= link_to 'Edit profile', edit_university_path(@university) %><br>
<%= link_to "Delete", 
	@university, 
	:method => :delete,
	:confirm => "Are you sure you want to delete this university?" %>
<br>
<%= link_to 'Back', {:action => 'index'} %>