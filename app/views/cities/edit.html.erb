<<<<<<< HEAD
<h1>Edit City</h1>

<hr />

<p class="text-info">Does this city have any mistake or field outdated? Help us, and feel free to modify it :)</p>

<hr />

<%= form_for @city, :html => { :class => "form-horizontal" } do |u| %>

	<fieldset>
	    <div class="control-group">
	        <%= u.label :name, "Name", :class => "control-label" %>
	        <div class="controls">
	            <%= u.text_field :name, :placeholder => "Name" %>
	        </div>
	    </div>

		<div class="control-group">
		     <%= u.label :country_id, "Country", :class => "control-label" %>
		    <div class="controls">
		        <%= u.select :country_id, [["Select one", -1]].concat(@countries), :class => "city-dropdown", :select => :city_id %>
		    </div>
		</div>

	    <div class="control-group">
	        <%= u.label :description, "Description", :class => "control-label" %>
	        <div class="controls">
	            <%= u.text_area :description, :placeholder => "Description", :rows => 7 %>
	        </div>
	    </div>

  	</fieldset>

    <div class="form-actions">
		<%= u.submit "Update", :class => "btn btn-primary" %>
		<%= link_to "Back", :back, :class => "btn" %>
	</div>

<% end %>
=======
<% # This view displays a form for a user to edit an existing City %>
<h1><%= @city.name %></h1>

<hr>

<p class="text-info">Changing any of the following values will instantly update the corresponding city. If you change the administrator, you won't be able to edit the city anymore.</p>

<hr>

<%= form_for @city, :html => { :class => "form-horizontal" } do |f| %>
	<%= render :partial => "city_edit", :locals => {
		:f => f,
		:countries => @countries,
		:mode => :edit
	} %>

	<div class="form-actions">
		<%= f.submit "Save", :class => "btn btn-primary" %>
		<%= link_to "Cancel", @city, :class => "btn" %>
	</div>
<% end %>

<ul class="breadcrumb">
	<li><%= link_to "Home", root_url %> <span class="divider">/</span></li>
	<li><%= link_to "Cities", cities_path %> <span class="divider">/</span></li>
	<li><%= link_to @city.name, @city %> <span class="divider">/</span></li>
	<li class="active">Edit</li>
</ul>
>>>>>>> 94a21600b3649cc3fa6da8665f780a7b400c2687
