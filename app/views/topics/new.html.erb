<%= form_for ([@city, @topic]) do |t| %>

<table>
<tr>
	<td>Title:</td>
	<td><%= t.text_field :title %></td>
</tr>
<tr>
	<td>Description:</td>
	<td><%= t.text_area :description %></td>
</tr>
</table>
<%= t.submit "submit" %><br>

<%= link_to 'Back', {:action => 'index' } %>
<% end %>