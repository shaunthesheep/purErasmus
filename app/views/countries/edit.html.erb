<%= form_for @country do |uForm| %>
<table>
	<tr>
		<td>Name:</td>
		<td><%= uForm.text_field :name %></td>
	</tr>
</table>
<%= uForm.submit "Update" %><br>
<%= link_to 'Back', {:action => 'show', :id => @country } %>
<% end %>